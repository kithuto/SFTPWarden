version: 1
project:
  name: yaml-example
provider:
  type: yaml
  path: /etc/sftpwarden/users.yaml
  user_schema: 2
healthcheck:
  interval_seconds: 30
  timeout_seconds: 10
  retries: 3
  start_period_seconds: 20
