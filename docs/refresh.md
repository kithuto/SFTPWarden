# Refresh

`refresh` asks a runtime to reload users immediately.

```bash
sftpwarden refresh -c dev
sftpwarden refresh --all
sftpwarden refresh -c prod --dry-run
```

`watch` syncs files. `refresh` applies provider state inside a running runtime.

